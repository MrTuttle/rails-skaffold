<div id="<%= dom_id flat %>" class="card p-4">
<%= image_tag(flat.picture_url, size:"100") %>
  <p>
    <strong>Name:</strong>
    <%= flat.name %>
  </p>

  <p>
    <strong>Address:</strong>
    <%= flat.address %>
  </p>

</div>
